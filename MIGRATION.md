# MIGRATION — Morphe → sokrates monorepo (Projection M)

## Why this repo is standalone
Velocity sandbox for agent-collaborative development: build fast outside the
monorepo's heavy CI guards, migrate in once close to complete. The guards are a
velocity tax during exploration, **not** the quality bar — we hold the bar here
ourselves (no `any`, a11y structural, tests green, lint clean) so migration day
is a lift, not a CI reckoning.

## What migrates (and what stays)
- **Migrates:** `src/lib/` — the extractable core:
  `grammar/ tokens/ context/ compounds/ dialects/ render/ primitives/`. The library.
- **Stays behind:** `src/routes/` — the SvelteKit demo + dignity-test harness. Throwaway scaffold.

## Landing zone (per the dependency lattice)
Morphe is **Projection M of Eidos** — the *perceivable* projection, beside
Projection A (relational / lakehouse) and Projection B (executable / runtime).
It lands at the **domain** layer, depending **downward only**:
- may depend on: `eidos` (the form it projects), `hyle` (matter), `logos`/`dynamis` (runtime), `sokrates_core` (foundation)
- depended on by: `nous_app`, `sokrates_ctl` (composition)
- must NOT import upward — enforced by the monorepo's layer guards on arrival.

## The Eidos-projection lift — done
The lift has already happened, inside this repo, ahead of the monorepo move:
1. The grammar's source of truth is a Python **Projection M**, `py/morphe_grammar/models.py`
   (Pydantic models).
2. `grammar/types.ts` is **generated** from that source — "one schema, three
   jobs": Pydantic validator + TS types (+ a constrained-decode mask still to
   come for the agent decoder). Generation runs via
   `python -m morphe_grammar.artifacts --write`; the file's header records its
   own provenance (`@generated by ...`, `Source of truth: py/morphe_grammar/models.py`).
   `just schema-check` runs the generator in `--check` mode and is a CI gate —
   drift between the Python source and the committed TS artifact fails the build.
3. `render/ context/ compounds/ tokens/ primitives/` remain unchanged by this —
   they *consume* the grammar, they don't define it.
Because the shadow was logic-free, this was mechanical, not a rewrite — exactly
as anticipated below. What's left for the monorepo landing is *where* the
Python package lives (in/near `eidos` rather than in this repo) and the
constrained-decode mask, not the codegen mechanism itself.

## Frontend / Python split
- Python Projection-M (grammar/schema) → domain layer near `eidos`.
- Svelte renderer → a frontend artifact consuming generated types; the appliance
  serves it. The browser was always the codegen consumer (the producer is
  Python / pydantic-ai), so this split is structural, not a compromise.

## Toolchain on arrival
- **bun** (matches the monorepo JS standard). `bun install`, `bun run check|test|build`.
- SvelteKit **adapter** is already chosen: `@sveltejs/adapter-vercel`, pinned,
  `runtime: "nodejs22.x"`. Re-evaluate only if the monorepo landing zone's
  deploy target differs from Vercel.
- **Self-host fonts.** The dignity demo pulls Newsreader / Hanken Grotesk / IBM
  Plex Mono + Material Symbols from a CDN. The appliance is on-prem (Sovereign
  SKU = no outbound), so **fonts must be self-hosted before migration** or the
  on-prem render breaks.

## Pre-migration close list (Phase-1 edges)
Owner's bar: **not just two colour dialects — structural divergence first.**
- [x] Compound dialects + `dialect.compounds[]` enforced at render (the
      `restrictCompounds` view, gated by the active dialect). Dialects differ
      in *structure*, not just palette.
- [ ] `Vary` mid-loop / objective optimizer (currently renders the default
      option only; the objective-optimizer loop is genuinely Phase 2).
- [x] Input `bind` wired to an external store (`src/lib/state/store.svelte.ts`
      — client store, tier-1/2 escalation, `ContextDigest`; no longer tier-0
      local only).
- [x] Public barrel `index.ts` re-exports `getDialect` / `DIALECTS` / all nine
      shipped dialects.
- [x] Self-host fonts (see above).
- [x] Choose a SvelteKit adapter — `@sveltejs/adapter-vercel`, pinned,
      `runtime: "nodejs22.x"`.

## Verify (local)
`bun run check` (→ 0/0) · `bun run test` (→ 329 tests across 21 files) ·
`bun run build` (client+SSR)
· `bun run dev` → http://localhost:5173/ (dignity test; toggle the dialect for
the on-screen Lemma-3 fixed point).
