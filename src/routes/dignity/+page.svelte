<script lang="ts">
	/*
	 * /dignity — the explicit Corollary-1 dignity-test route. Renders the shared
	 * DignityDemo (one hand-authored tree, a dialect toggle, no agent). The same
	 * component is mounted at `/` so `pnpm dev` lands on it directly.
	 */
	import DignityDemo from "../_demo/DignityDemo.svelte";
</script>

<DignityDemo />
